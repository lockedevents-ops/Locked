"use client";

// ScrollToTop has been intentionally disabled while the chat button replaces its position.
// Keeping a no-op export prevents import errors elsewhere in the codebase.

export function ScrollToTop(): null {
  return null;
}
